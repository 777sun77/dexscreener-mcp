{
  "name": "dexscreener-mcp",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0-beta.5",
    "agents": "0.20.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
